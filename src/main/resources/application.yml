spring:
  application:
    name: First SpringBoot App
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    initialization-mode: always
management:
  endpoint:
    health:
      enabled: true
      show-details: always
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"


